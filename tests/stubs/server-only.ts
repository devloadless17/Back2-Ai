/**
 * Test stub for the `server-only` package.
 *
 * The real module throws on import to stop server code being bundled into the
 * client. Under Vitest there is no client bundle, and the modules under test
 * are server modules by definition, so this stands in for it.
 */
export {};
